import 'package:flutter/material.dart';

class Guaidanceview extends StatelessWidget {
  const Guaidanceview({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Guaidance'),
      ),
    );
  }
}